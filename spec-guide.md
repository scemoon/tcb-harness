# Spec Guide — 需求规范化指南

## 目录

1. [EARS 语法规范](#ears-语法规范)
2. [小程序特有需求场景](#小程序特有需求场景)
3. [需求文档模板](#需求文档模板)
4. [规范校验规则](#规范校验规则)
5. [反馈迭代流程](#反馈迭代流程)
6. [技术方案模板](#技术方案模板)
7. [任务分解模板](#任务分解模板)

---

## EARS 语法规范

EARS (Easy Approach to Requirements Syntax) 用统一格式消除需求歧义。

### 五种模式

| 模式 | 语法 | 适用场景 |
|------|------|---------|
| **Ubiquitous** | The `{system}` shall `{response}` | 始终有效的需求 |
| **Event-Driven** | When `{trigger}`, the `{system}` shall `{response}` | 事件触发 |
| **Unwanted** | If `{unwanted condition}`, the `{system}` shall `{response}` | 异常处理 |
| **State-Driven** | While `{state}`, the `{system}` shall `{response}` | 条件持续 |
| **Optional** | Where `{feature}` is enabled, the `{system}` shall `{response}` | 可选功能 |

### 小程序示例

- **Ubiquitous:** "The 小程序 shall display a bottom tab bar on all main pages."
- **Event-Driven:** "When the user taps '提交订单', the 小程序 shall create an order record in CloudBase and navigate to order detail page."
- **Unwanted:** "If the CloudBase write operation fails, the 小程序 shall show a toast with error message and retain the user's form input."
- **State-Driven:** "While the user is logged in, the 小程序 shall display personalized content on the home page."
- **Optional:** "Where 订阅消息 is enabled, the 小程序 shall prompt the user to subscribe before order submission."

### 反模式（禁止出现）

- ❌ "系统应该快速响应" — 无量化标准
- ❌ "界面要好看" — 主观描述
- ❌ "处理好错误" — 未指定什么错误、怎么处理
- ❌ "参考XX应用的设计" — 需求不能依赖外部参照

---

## 小程序特有需求场景

### 页面栈管理

小程序页面栈最大 10 层。需求涉及多级跳转时必须标注：

```
页面栈深度: 首页(1) → 列表(2) → 详情(3) → 表单(4) → 预览(5)
```

### 分享与转发

明确分享行为：
- 分享到聊天 vs 分享到朋友圈
- 分享卡片标题/图片/路径
- 是否需要自定义分享图

### 订阅消息

涉及订阅消息的需求必须包含：
- 消息模板 ID
- 触发时机
- 用户拒绝订阅的降级方案

### 云开发约束

- 单次数据库查询上限 20 条（小程序端），需分页时明确
- 云函数超时上限 20s（默认 3s），长任务需标注
- 云存储文件大小限制

### 用户登录

小程序端 CloudBase 登录通常无需用户操作（静默登录获取 OPENID），需求应明确：
- 是否需要获取手机号/用户信息
- 是否需要自定义用户资料

---

## 需求文档模板

```markdown
# 需求文档 — {功能名称}

## 1. 概述

{一段话描述业务背景和目标}

## 2. 用户角色

| 角色 | 描述 |
|------|------|
| 游客 | 未登录用户，可浏览公开内容 |
| 注册用户 | 已登录用户，可使用全部功能 |

## 3. 功能需求

### FR-01: {需求名称}

**优先级:** P0 / P1 / P2

**用户故事:** 作为{角色}，我希望{功能}，以便{价值}。

**验收标准:**

1. When {触发条件}, the 小程序 shall {系统响应}.
2. If {异常条件}, the 小程序 shall {异常处理}.
3. While {持续条件}, the 小程序 shall {持续行为}.

**页面栈影响:** {新页面/替换页面/无变化}

**依赖:** {依赖的其他需求 ID 或外部系统}

### FR-02: ...

## 4. 非功能需求

### NFR-01: 性能

- 首屏加载时间 ≤ {N}s（WiFi 环境）
- 列表滚动帧率 ≥ {N}fps

### NFR-02: 兼容性

- 支持微信版本 ≥ {version}
- 支持 iOS / Android 双平台

### NFR-03: 安全

- 用户敏感数据传输加密
- CloudBase 安全规则配置

## 5. 数据需求

| 数据实体 | 来源 | 存储位置 | 访问模式 |
|---------|------|---------|---------|
| 用户信息 | 小程序授权 | CloudBase NoSQL | 读写 |
| ... | ... | ... | ... |

## 6. 交互需求

{引用 ui-design-guide.md 中的设计系统，明确设计风格}

- 设计风格: {极简/商务/活泼/...}
- 主色调: #{color}
- 参考组件: {CloudBase UI 组件库中的组件}

## 7. 约束与假设

- 约束1: ...
- 假设1: ...
```

---

## 规范校验规则

`validate_spec.py` 检查以下规则：

### 必须通过 (P0)

| 规则 ID | 规则描述 | 检查方式 |
|---------|---------|---------|
| S-001 | 每个功能需求有 EARS 格式验收标准 | 正则匹配 "shall" + 完整语法 |
| S-002 | 无主观/模糊词汇（快速、好看、友好等） | 黑名单词汇检测 |
| S-003 | 每个需求有优先级标注 (P0/P1/P2) | 字段存在性检查 |
| S-004 | 涉及多页面的需求标注页面栈深度 | 页面路径分析 |
| S-005 | 用户角色已定义 | "用户角色" section 存在 |

### 建议通过 (P1)

| 规则 ID | 规则描述 |
|---------|---------|
| S-101 | 异常场景覆盖率（每 3 个正常流程至少 1 个异常） |
| S-102 | 非功能需求已量化（有具体数值） |
| S-103 | 数据实体与 CloudBase 存储方案匹配 |
| S-104 | 交互需求引用了设计系统 |

### 输出格式

```
✅ S-001: PASS (12/12 requirements have EARS criteria)
❌ S-002: FAIL — Found vague terms: "快速响应" (FR-03), "友好提示" (FR-05)
⚠️  S-101: WARN — FR-01/02/03 have no exception scenarios
```

---

## 反馈迭代流程

### Review 清单

对每条需求检查：

1. **可测性** — 能否设计出明确的 pass/fail 测试？
2. **完整性** — 正常 + 异常 + 边界是否覆盖？
3. **一致性** — 与其他需求是否矛盾？
4. **可行性** — 小程序/CloudBase 技术上能否实现？
5. **可追踪** — 验收标准能否追溯到用户故事？

### 反馈模板

```markdown
# Spec Review — 第 {N} 轮

## 总体评价

- 通过需求: {N}/{total}
- 需修改需求: {N}/{total}
- 阻塞问题: {N}

## 逐条反馈

### FR-01 — {需求名称}
- ✅ 验收标准清晰
- ⚠️  缺少网络断开场景
- 建议: 增加 "If network is unavailable, the 小程序 shall cache the submission and retry on reconnect."

### FR-03 — {需求名称}
- ❌ "快速响应" 不可测
- 建议: 改为 "the 小程序 shall display loading state within 500ms and complete data load within 3s (WiFi)"
```

### 迭代规则

- 最多 3 轮 review（第 4 轮强制 escalate 给用户决策）
- 每轮只反馈未通过项，已通过项不再重复
- 用户确认 = 最终通过，不再自动 review

---

## 需求交流机制

AI应在以下时机主动提问和反馈：

### 2.1 需求接收阶段（Requirement）

When receiving requirements from the user, AI should:
1. **复述理解** - 用自己的话复述用户需求，确认理解正确
2. **提出疑问** - 对每个功能点提出见解和疑问：
   - 这个功能的核心价值是什么？解决了什么问题？
   - 有哪些边界条件需要考虑？
   - 与现有功能的关系？是否与FR-XX冲突？
   - 性能/安全/兼容性有无特殊要求？
3. **给出建议** - 基于最佳实践给出实现建议
4. **确认范围** - 明确MVP范围，区分"必须有"和"可以有"

### 2.2 设计评审阶段（Design Review）

After writing design.md, AI should:
1. **展示设计方案** - 给出完整的架构图和数据流
2. **征求反馈** - 询问用户对方案的意见
3. **讨论备选** - 提供1-2个备选方案并说明权衡

### 2.3 反馈格式

交互式反馈示例：
```
我理解您需要实现"商品评论"功能。以下是我的疑问和建议：

🤔 **关于FR-03商品评论功能**

1. **核心目标**：这个功能是为了让购买过的用户分享体验，还是为了帮助其他用户做购买决策？

2. **权限问题**：游客是否可以查看评论？只有购买后才能评论？
   - 我的建议：游客可查看但需登录后评论，平衡体验与互动性

3. **边界条件**：
   - 一个用户对同一商品是否可以多次评论？（覆盖 vs 追加）
   - 评论是否可以修改/删除？修改后是否需要审核？
   - 是否支持图片/视频评论？CloudBase存储有成本考虑

4. **与FR-02的关系**：FR-02是商品展示，FR-03是用户互动。建议分离为两个独立功能点。

5. **我的建议**：
   - Phase 1: 先实现文字评论（MVP），图片评论作为Phase 2
   - 使用云函数实现，避免小程序端直接写数据库（安全考虑）

请确认以上理解是否正确，或者补充/修改上述需求。
```

---

## 技术方案模板

```markdown
# 技术方案 — {功能名称}

## 1. 架构概览

{Mermaid 架构图}

## 2. 技术选型

| 层 | 技术 | 原因 |
|----|------|------|
| 前端框架 | 原生小程序 / Taro / uni-app | {选择原因} |
| 状态管理 | {方案} | {原因} |
| 后端 | CloudBase 云函数 | {原因} |
| 数据库 | CloudBase NoSQL / MySQL | {原因} |
| 存储 | CloudBase 云存储 | {原因} |

## 3. 数据模型

{集合/表设计，含字段类型和索引}

## 4. API 设计

{云函数接口设计，含请求/响应格式}

## 5. 安全规则

{CloudBase 安全规则配置}

## 6. 测试策略

- 单元测试覆盖: 核心逻辑 ≥ 80%
- 集成测试: 云函数 + 数据库联调
- E2E 测试: 关键用户路径

## 7. 风险与缓解

| 风险 | 概率 | 影响 | 缓解方案 |
|------|------|------|---------|
| ... | ... | ... | ... |
```

---

## 任务分解模板

```markdown
# 实现计划 — {功能名称}

- [ ] 1. {任务名称}
  - {具体步骤}
  - {具体步骤}
  - _需求: FR-01, FR-02_

- [ ] 2. {任务名称}
  - {具体步骤}
  - _需求: FR-03_

## 任务依赖图

{Mermaid 流程图展示任务间依赖}

## 估算

| 任务 | 复杂度 | 预估 |
|------|--------|------|
| 1 | 中 | 2h |
| 2 | 高 | 4h |
```
